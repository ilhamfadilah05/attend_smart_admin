import 'package:flutter/material.dart';

class NavbarPages extends StatelessWidget {
  const NavbarPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(10),
    );
  }
}
